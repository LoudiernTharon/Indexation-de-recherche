\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{enumitem}

\geometry{margin=2cm}
\setlength{\parskip}{0.3em}

\title{\textbf{TP4 - Indexation de Texte par ABR}}
\author{Timothée POUPINEAU & Loudiern THARON}
\date{\today}

\begin{document}

\maketitle

\section{Structures et Fonctions Supplémentaires}

\subsection{Structures Supplémentaires}

En plus des structures demandées (\texttt{T\_Position}, \texttt{T\_Noeud}, \texttt{T\_Index}), nous avons implémenté :

\begin{itemize}[noitemsep]
    \item \textbf{T\_MotOrdre} (B.6) : Liste chaînée stockant un mot et son ordre dans la phrase. Permet de reconstruire une phrase en ordre croissant lors de l'affichage des occurrences.
    \item \textbf{T\_Phrase} (B.7) : Tableau dynamique contenant les mots d'une phrase avec leurs ordres. Facilite la reconstruction complète du texte.
\end{itemize}

\textbf{Justification} : Ces structures évitent des parcours répétés de l'ABR et permettent de trier les mots efficacement pour reconstituer les phrases dans le bon ordre.

\subsection{Fonctions Auxiliaires}

\begin{itemize}[noitemsep]
    \item \texttt{initialiserIndex()} : Initialise un index vide
    \item \texttt{convertirMinuscules()} : Ignore la casse (voiture = Voiture = VOITURE)
    \item \texttt{libererPositions()}, \texttt{libererNoeud()}, \texttt{libererIndex()} : Libération mémoire
    \item \texttt{afficherNoeudInfixe()} : Parcours infixe pour affichage alphabétique
    \item \texttt{ajouterMotOrdre()} : Insertion triée dans une liste de mots ordonnés
    \item \texttt{collecterMotsPhrase()} : Collecte les mots d'une phrase donnée
    \item \texttt{afficherPhrase()}, \texttt{libererMotsOrdres()} : Affichage et libération pour B.6
    \item \texttt{ajouterMotPhrase()}, \texttt{collecterPhrasesRecursif()} : Collecte pour B.7
\end{itemize}

\textbf{Justification} : Modularité du code, réutilisation des fonctions et gestion rigoureuse de la mémoire dynamique.

\section{Complexité des Fonctions}

\subsection{Fonctions Demandées}

\begin{tabular}{|l|l|l|}
\hline
\textbf{Fonction} & \textbf{Complexité} & \textbf{Remarque} \\
\hline
\texttt{ajouterPosition()} & $O(p)$ & $p$ = nb positions du mot \\
\hline
\texttt{ajouterOccurence()} & $O(h + p)$ & $h$ = hauteur ABR \\
\hline
\texttt{indexerFichier()} & $O(m \times h)$ & $m$ = nb mots fichier \\
\hline
\texttt{afficherIndex()} & $O(n \times p)$ & $n$ = mots distincts \\
\hline
\texttt{rechercherMot()} & $O(h)$ & Recherche binaire \\
\hline
\texttt{afficherOccurencesMot()} & $O(k \times n)$ & $k$ = occurrences du mot \\
\hline
\texttt{construireTexte()} & $O(n \times p + s^2)$ & $s$ = nb phrases \\
\hline
\end{tabular}

\vspace{0.5em}
\textbf{Notation} : $h$ = hauteur de l'arbre. Cas moyen : $h = O(\log n)$. Pire cas : $h = O(n)$.

\subsection{Fonctions Auxiliaires}

\begin{tabular}{|l|l|}
\hline
\textbf{Fonction} & \textbf{Complexité} \\
\hline
\texttt{convertirMinuscules()} & $O(k)$ ($k$ = longueur mot) \\
\hline
\texttt{libererIndex()} & $O(n)$ \\
\hline
\texttt{collecterMotsPhrase()} & $O(n \times p)$ \\
\hline
\texttt{ajouterMotOrdre()} & $O(m)$ ($m$ = mots/phrase) \\
\hline
\texttt{collecterPhrasesRecursif()} & $O(n \times p)$ \\
\hline
\texttt{ajouterMotPhrase()} & $O(1)$ amorti \\
\hline
\end{tabular}

\section{Choix d'Implémentation}

\begin{itemize}[noitemsep]
    \item \textbf{Casse} : Conversion systématique en minuscules via \texttt{tolower()}.
    \item \textbf{Ordre} : \texttt{strcmp()} pour l'ordre lexicographique dans l'ABR.
    \item \textbf{Positions triées} : Insertion en $O(p)$ mais évite des tris ultérieurs.
    \item \textbf{Mémoire} : Tableaux dynamiques avec doublement de capacité. Libération complète à la fin.
    \item \textbf{B.6 optimisé} : Pour chaque occurrence, parcours unique de l'ABR pour collecter les mots de la phrase concernée, insertion triée par ordre.
    \item \textbf{B.7} : Collecte globale des phrases puis tri par numéro et par ordre des mots.
\end{itemize}

\end{document}
